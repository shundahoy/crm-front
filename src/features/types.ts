export interface LOGIN_USER {
  id: number;
  name: string;
  email: string;
}
export interface JWT {
  jwt: string;
}

export interface LOGIN_FORM {
  email: string;
  password: string;
}
